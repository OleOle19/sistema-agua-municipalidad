{
  "name": "plataforma-municipal-servicios-publicos",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma full stack para la gestión municipal de agua, energía, caja y trabajo de campo.",
  "scripts": {
    "setup": "npm ci --prefix server && npm ci --prefix client",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix client",
    "migrate": "npm run migrate --prefix server && npm run migrate:luz --prefix server",
    "test": "npm test --prefix server && npm test --prefix client",
    "lint": "npm run lint --prefix client",
    "build": "npm run build --prefix client",
    "validate": "npm test && npm run lint --prefix client && npm run build --prefix client && npm run check:bundle --prefix client"
  },
  "engines": {
    "node": ">=22"
  }
}
