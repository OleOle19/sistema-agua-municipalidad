// Las acciones se ejecutan directamente; se conserva esta función para no
// duplicar cambios en los flujos existentes que antes solicitaban confirmación.
export const confirmAction = async () => true;
